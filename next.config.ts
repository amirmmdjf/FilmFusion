// @ts-check
 
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['media.themoviedb.org'], // Add the allowed image domain here
  },
  /* config options here */
}
 
module.exports = nextConfig